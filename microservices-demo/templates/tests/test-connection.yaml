apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservices-demo.fullname" . }}-test-connection"
  labels:
    {{- include "microservices-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microservices-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
