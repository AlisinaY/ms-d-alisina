apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservices-demo.fullname" . }}
  labels:
    {{- include "microservices-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microservices-demo.selectorLabels" . | nindent 4 }}
